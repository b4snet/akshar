# akshar-frontend

React + TypeScript + Vite SPA. Phase 004 ships the toolchain only — no product features yet.

All commands run from the repository root: `npm run setup`, `npm run dev`, `npm run lint`, `npm run typecheck`, `npm run test`, `npm run build`.

The authoritative documentation set lives in [`/docs`](../docs/README.md).
