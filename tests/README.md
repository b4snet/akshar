# Repository-Level Tests

Cross-stack test suites that exercise frontend and backend together:

- End-to-end browser suites (Playwright) arrive with their own phase and live
  here.

Per-stack suites remain in `frontend/src/**` and `backend/tests/**`; do not
move stack-local tests up a level.
