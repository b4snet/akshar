{
  "name": "akshar",
  "version": "0.1.0",
  "private": true,
  "description": "AKSHAR — Education Operating System for Nepal. Root toolchain orchestration (Phase 004).",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "setup": "node scripts/setup.mjs",
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "preview": "npm --prefix frontend run preview",
    "lint": "npm --prefix frontend run lint && node scripts/run-backend.mjs lint",
    "lint:fe": "npm --prefix frontend run lint",
    "lint:be": "node scripts/run-backend.mjs lint",
    "typecheck": "npm --prefix frontend run typecheck && node scripts/run-backend.mjs analyse",
    "typecheck:fe": "npm --prefix frontend run typecheck",
    "typecheck:be": "node scripts/run-backend.mjs analyse",
    "format": "npm --prefix frontend run format",
    "format:check": "npm --prefix frontend run format:check && node scripts/run-backend.mjs lint",
    "test": "npm --prefix frontend run test -- --run && node scripts/run-backend.mjs test",
    "test:fe": "npm --prefix frontend run test -- --run",
    "test:be": "node scripts/run-backend.mjs test",
    "secrets": "node scripts/secret-scan.mjs",
    "reset": "node scripts/reset.mjs",
    "seed": "echo \"seed: no database is provisioned yet; seeding becomes available in a later phase.\""
  }
}
