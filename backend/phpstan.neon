includes:
    - ./vendor/nunomaduro/larastan/extension.neon

parameters:

    paths:
        - app
        - config
        - database
        - routes
        - tests

    # Strict enough to catch real defects while allowing Laravel's dynamic
    # surface; tighten as modules stabilize.
    level: 6

    treatPhpDocTypesAsCertain: false
